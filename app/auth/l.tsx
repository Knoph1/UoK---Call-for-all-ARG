 // useEffect(() => {
  //   // Check route permissions
  //   if (session?.user && !canAccessRoute(session.user.role, pathname)) {
  //     redirect("/dashboard");
  //   }
  // }, [session, pathname]);